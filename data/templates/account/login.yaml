# 账户登录接口 YAML 模板
# 对应接口: POST /api/v1/auth/login

api_info:
  path: "/api/v1/auth/login"
  method: "POST"
  description: "用户登录获取 Token"

logic_rules: []

payload:
  username: "test_admin"
  password: "Test@123456"
  channel: "${choice(['APP', 'WEB'])}"
  device_id: "${uuid}"
  captcha: ""
  trace_id: "${uuid}"
  request_time: "${timestamp}"
